apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: ambient-code
type: Opaque
stringData:
  # MinIO credentials for e2e testing
  root-user: "minioadmin"
  root-password: "minioadmin123"
  access-key: "minioadmin"
  secret-key: "minioadmin123"

